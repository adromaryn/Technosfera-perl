% layout 'default';
% title 'Add table';

%= form_for '/table' => (method=>'POST') => begin
  <h3>Add your tracks with table</h3>
  %= text_area 'table', id => "table", cols => 50, rows => 10
  <br />
  %= submit_button "Add", id => "submit", class => "btn btn-primary"
%= end
